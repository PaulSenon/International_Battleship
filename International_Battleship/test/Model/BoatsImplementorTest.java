package Model;

import org.junit.Before;
import org.junit.Test;
import tools.Coord;

import static org.junit.Assert.*;

public class BoatsImplementorTest {

    private BoatsImplementor boatsImplementor;

    @Before
    public void setUp() throws Exception {
//        this.boatsImplementor = new BoatsImplementor();
    }

    @Test
    public void rotateAround() {
        // TODO @Paul WIP
//        Coord coord = new Coord(10,10);
//        Coord pivot = new Coord(8,10);
//        coord = this.boatsImplementor.rotateAround(coord, pivot, (float)-Math.PI);
//
//        assertEquals(new Coord(6,10), coord);
    }
}